"""
SAP Modular Skills Package
Built from the 9,792-line API map (R28-R40)

Modules:
  sap_core       — Base HTTP client, cookie relay, session management
  sap_clients    — Client CRUD, search, contacts, tags, properties
  sap_jobs       — Job/service CRUD, packages, one-time, recurring
  sap_estimates  — Estimate CRUD, line items, accept/decline, convert
  sap_dispatch   — Dispatch board query, assignment, completion, routing
  sap_invoices   — Invoice CRUD, void, complete, mail, merge, bulk pay
  sap_payments   — Payment CRUD, void, restore, applied items
  sap_employees  — Employee CRUD, time off, timeline, notes, touchpoints
  sap_vendors    — Vendor CRUD, scheduling, damage cases, ratings
  sap_contracts  — Contract CRUD, generate invoices, scheduled services
  sap_credits    — Credit CRUD, line items, tax amounts
  sap_expenses   — Expense CRUD, export, purchase orders
  sap_projects   — Project CRUD, milestones, invoicing
  sap_forms      — Form queries, response data, mappings
  sap_messaging  — Text messages, message center, response templates
  sap_artifacts  — File attachments, photos, documents
  sap_automations— Automation CRUD, copy, status updates
  sap_reports    — Report center, batch exports
  sap_calendar   — Calendar events, scheduling
  sap_tags       — Tag CRUD, categories, permissions
  sap_lists      — All lookup lists (service types, products, etc.)
  sap_touchpoints— Touchpoint CRUD, status, types
"""
